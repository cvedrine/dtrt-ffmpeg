#! /usr/bin/zsh

set -e
set -- ${(z)SSH_ORIGINAL_COMMAND}
# temporary=$( mktemp -d ~/pod.XXXXXXXXXXXXXXX )
temporary=pod.encoding.$1
test -d $temporary ||
    mkdir  $temporary
cd $temporary
print -l "$@" > X
sbatch -o log.stdout -e log.stderr ffmpod "$@"
